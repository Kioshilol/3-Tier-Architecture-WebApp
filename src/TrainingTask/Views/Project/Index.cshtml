@model TrainingTask.ViewModels.IndexViewModel<ProjectViewModel>
@{
    ViewData["Title"] = "Project";
}
<h2>@ViewData["Title"]</h2>

<body>
    <div class="form-group">
        <a asp-action="Edit" asp-controller="Project" class="btn btn-default">Create</a>
        <a asp-action="UploadToXML" asp-controller="Project" class="btn btn-default">XML</a>
        <a asp-action="UploadToExcel" asp-controller="Project" class="btn btn-default">Excel</a>
    </div>
    <table class="table table-striped">
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>ShortName</td>
            <td>Description</td>
            <td>Edit</td>
            <td>Delete</td>
        </tr>
        @foreach (var item in Model.ViewModelList)
        {
            <tr>
                <td>@item.Id</td>
                <td><a asp-action="Details" asp-controller="Project" asp-route-id="@item.Id">@item.Name</a></td>
                <td>@item.ShortName</td>
                <td>@item.Description</td>
                <td><a asp-action="Edit" asp-controller="Project" asp-route-id="@item.Id">Edit</a></td>
                <td><a asp-action="Delete" asp-controller="Project" asp-route-id="@item.Id">Delete</a></td>
            </tr>
        }

    </table>
    <div class="form-group">
        @for (int i = 1; i <= Model.Page.TotalPages; i++)
        {
            <a asp-action="Index" asp-controller="Project" asp-route-page="@i" class="btn btn-default">@i</a>
        }
    </div>
</body>
